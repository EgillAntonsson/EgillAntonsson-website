import { SoundManager } from './soundManager'

describe('sound common', () => {
	// let pipe: SoundCommon

	beforeEach(() => {
		// pipe = new MyNumberPipe()
	})

	it('test cases', () => {

		console.log('bla bla')
	// 	expect(pipe.transform(2191011.8, 0, '/', 1000)).toBe('2\'191')
	// 	expect(pipe.transform(2191011.8, 1)).toBe('2\'191\'011.8')
	// 	expect(pipe.transform(580600)).toBe('580\'600')
	// 	expect(pipe.transform(12.3, 1, '*', 1000)).toBe('12\'300.0')
	// 	expect(pipe.transform(0.5, 1)).toBe('0.5')
	// 	expect( function() {pipe.transform(0.5, 0, '+')}).toThrow(new RangeError('parameter "conversionSign" can only have value "/" or "*"'))
	})
})
